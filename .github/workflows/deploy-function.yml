# .github/workflows/deploy-function.yml
name: Deploy Azure Function

on:
  push:
    paths:
      - 'AzureServicesInAction/Functions/**'
      - '.github/workflows/deploy-function.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore AzureServicesInAction/Functions/Functions.csproj

    - name: Build project
      run: dotnet build AzureServicesInAction/Functions/Functions.csproj --configuration Release

    - name: Publish project
      run: dotnet publish AzureServicesInAction/Functions/Functions.csproj --configuration Release --output ./publish

    - name: Deploy to Azure Function App
      uses: azure/functions-action@v1
      with:
        app-name: raskinblobtrigger
        package: ./publish
        publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}